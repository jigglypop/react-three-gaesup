export type ICategory = {
  id: number;
  title: string;
  posts: number;
};

export type ICategoryTag = {
  id: number;
  title: string;
  thumbnail: string;
  posts: number;
};

export type ICategoryHeader = {
  id: number;
  title: string;
  posts: number;
};
