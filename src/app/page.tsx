import Main from 'component/main';

export const metadata = {
  title: 'react-three-gaesup | example',
  description: 'react-three-gaesup | example use next.js'
};

export default function Page() {
  return <Main />;
}
